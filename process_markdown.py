this is the script that runs gptr main.py to produces the repprts, one at a time based on a specifyed folder. then it points llm_doc_eval to the reports and writes the best one to the configured dir.
